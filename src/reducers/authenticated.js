export const initialState = {
  authenticated: false
};

const authenticated = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default authenticated;
